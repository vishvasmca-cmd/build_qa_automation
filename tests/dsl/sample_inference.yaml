test_name: inference_test
base_url: https://example.com/login

steps:
  # Using semantic names instead of locators
  - action: fill
    locator: username
    value: myuser

  - action: fill
    locator: password
    value: mypass

  - action: click
    locator: submit
    wait_for: visible
